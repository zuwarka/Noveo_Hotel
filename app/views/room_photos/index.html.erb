<h1>Room photos</h1>

<div id="room_photos">
  <% @room_photos.each do |room_photo| %>
    <%= render room_photo %>
    <p>
      <%= link_to "Show this room photo", room_photo %>
    </p>
  <% end %>
</div>